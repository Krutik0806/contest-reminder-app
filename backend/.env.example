# Backend Environment Variables Template
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000

# Database Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/contest-reminder

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/contest-reminder?retryWrites=true&w=majority

# JWT Secret Key
# Generate a random secret key for JWT tokens
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# VAPID Keys for Push Notifications
# Generate these keys by running: npx web-push generate-vapid-keys
# Then copy the public and private keys here
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@example.com

# Email Configuration (Optional - for email notifications)
# If not configured, emails will be logged to console instead
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# ================================
# SETUP INSTRUCTIONS:
# ================================

# 1. Generate VAPID Keys:
#    cd backend
#    npx web-push generate-vapid-keys
#    Copy the output to VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY above

# 2. Generate JWT Secret:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    Copy the output to JWT_SECRET above

# 3. MongoDB Setup:
#    Option A - Local:
#      - Install MongoDB Community Edition
#      - Use: mongodb://localhost:27017/contest-reminder
#    
#    Option B - Cloud (MongoDB Atlas):
#      - Create free cluster at mongodb.com/cloud/atlas
#      - Get connection string
#      - Replace username, password, and cluster URL

# 4. Change VAPID_SUBJECT to your email

# ================================
# SECURITY NOTES:
# ================================
# - NEVER commit this file to git
# - Use different secrets for development and production
# - Keep your VAPID private key secure
# - Change all default values before deploying
